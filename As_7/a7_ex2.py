# K11720457
# Aral Cimcim

# Create a class Rotate that rotates the matrix.

from a7_ex1 import Radian
import math

# class Rotate:

#     def __init__(self, matrix: list, degree: float, inplace=False):
#         self.matrix = matrix
#         self.degree = degree
#         self.inplace = inplace

#     def rotation(self) -> list[list] | None:
#         length = len(self.matrix)
#         radian = Radian(self.degree).rad()
#         sin_val = math.sin(radian)
#         cos_val = math.cos(radian)
#         center = length // 2

#         rotated_matrix = self.matrix